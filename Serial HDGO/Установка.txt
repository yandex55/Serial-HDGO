1. Залить все файлы себе на сервер, предварительно изменив название папки Default на название своего шаблона.
2. Запустить install.php после выполнения удалить файл.
3. В fullsoty.tpl вставить в любом месте <div id="ajaxPlayerm"></div>
4. В main.tpl в нужном месте вставить {include file="engine/mod_gameer/serial_hdgo/serial_hdgo_block.php"}
5. Открыть /engine/modules/main.php (если нет файла то /index.php в корне сайта) найти:
if ( $dle_module == "showfull" AND $news_found ) {
Ниже вставить:
$tpl->set ( '{newsid}', intval($_GET["newsid"]) );
6. Перед тегом </head> вставить <link rel="stylesheet" href="{THEME}/serial_hdgo/player.css" />
7. Перед тегом </body> вставить
[aviable=showfull]<script type="text/javascript">
	$(document).ready(function(){
		$.post(dle_root+"engine/mod_gameer/serial_hdgo/serial_hdgo_ajax.php", {news_id:{newsid}, player:"yes"}, function(data){
			if(data){
				$("#ajaxPlayerm").html(data);
				$('#simple-episodes-list').scrollLeft($('.b-simple_episode__item.active').position().left);
			}
		});
	});
</script>
<script type="text/javascript" src="{THEME}/serial_hdgo/player.js"></script>[/aviable]
8. Зайти в админ панель, настроить модуль.